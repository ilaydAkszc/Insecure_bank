@model Insecure_Bank.Models.ViewModels.TransferViewModel
@{
    ViewData["Title"] = "Para Transferi";
    var accounts = ViewBag.Accounts as List<Insecure_Bank.Models.Model.Account>;
}

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header bg-success text-white">
                <h3>💸 Para Transferi</h3>
            </div>
            <div class="card-body">
                @if (ViewBag.Message != null)
                {
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <strong>Başarılı!</strong> @ViewBag.Message
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                }

                <!-- KASITLI CSRF ZAFİYETİ - Antiforgery token yok! -->
                <form asp-action="Transfer" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="form-group mb-3">
                        <label asp-for="FromAccountId" class="form-label">
                            <i class="bi bi-bank"></i> Gönderen Hesap
                        </label>
                        <select asp-for="FromAccountId" class="form-control form-select">
                            <option value="">--Bir hesap seçiniz--</option>
                            @if (accounts != null)
                            {
                                @foreach (var account in accounts)
                                {
                                    <option value="@account.AcId">
                                        @account.AccountNumber - @account.AccountType (Bakiye: @account.Balance.ToString("N2")TL)
                                    </option>
                                }
                            }
                        </select>
                        <span asp-validation-for="FromAccountId" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="ToAccountNumber" class="form-label">
                            <i class="bi bi-arrow-right"></i> Alıcı Hesap Numarası
                        </label>
                        <input asp-for="ToAccountNumber" class="form-control" placeholder="Alıcı hesap numarası seçin (e.g., 1001, 1002)" />
                        <span asp-validation-for="ToAccountNumber" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="Amount" class="form-label">
                            <i class="bi bi-currency-dollar"></i> Miktar
                        </label>
                        <input asp-for="Amount" class="form-control" type="number" step="0.01" min="0.01" placeholder="0.00" />
                        <span asp-validation-for="Amount" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="Description" class="form-label">
                            <i class="bi bi-chat-text"></i>Açıklama
                        </label>
                        <input asp-for="Description" class="form-control"  />
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-success btn-lg">
                            <i class="bi bi-send"></i> Para Gönder
                        </button>
                        <a href="@Url.Action("ShowAccounts", "Banking")" class="btn btn-outline-secondary">
                            İptal
                        </a>
                    </div>
                </form>
            </div>
        </div>

       @*  <div class="alert alert-danger mt-4">
            <h6>🔓 CSRF Vulnerability Demonstration</h6>
            <p class="mb-2">This form has no CSRF protection! An attacker could:</p>
            <ul class="mb-2">
                <li>Create a malicious website with hidden forms</li>
                <li>Trick users into visiting the malicious site</li>
                <li>Automatically submit transfers without user consent</li>
            </ul>
            <small class="text-muted">
                Missing security measures:<br>
                • No <code>@Html.AntiForgeryToken()</code><br>
                • No <code>[ValidateAntiForgeryToken]</code> attribute<br>
                • No SameSite cookie settings
            </small>
        </div> *@
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

 @*    <script>
        // Demo: Show CSRF vulnerability in console
        console.log("CSRF Vulnerability: This form can be submitted from any external website!");
        console.log("Try the CSRF attack demo page to see this in action.");

        // Auto-fill demo values for testing
        document.addEventListener('DOMContentLoaded', function() {
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('demo') === 'true') {
                document.querySelector('select[name="FromAccountId"]').value = '2';
                document.querySelector('input[name="ToAccountNumber"]').value = '1001';
                document.querySelector('input[name="Amount"]').value = '10.00';
                document.querySelector('input[name="Description"]').value = 'Demo transfer';
            }
        });
    </script> *@
}
